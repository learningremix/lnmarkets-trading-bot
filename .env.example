# ===========================================
# LN Markets Trading Bot - Environment Variables
# ===========================================
# 
# Only DATABASE_URL is required here.
# ALL other settings are stored in the database and
# configurable via the /api/settings endpoint.
#
# ===========================================

# Database Connection (REQUIRED)
DATABASE_URL=postgresql://postgres:tradingbot@localhost:5432/lnmarkets_bot

# Optional: Docker Compose postgres password
POSTGRES_PASSWORD=tradingbot

# ===========================================
# After starting the server, configure via API:
# ===========================================
#
# Configure LN Markets:
# curl -X POST http://localhost:3000/api/settings \
#   -H "Content-Type: application/json" \
#   -d '{
#     "action": "configure_lnmarkets",
#     "apiKey": "your-api-key",
#     "apiSecret": "your-api-secret",
#     "passphrase": "your-passphrase",
#     "network": "testnet"
#   }'
#
# Configure TradingView:
# curl -X POST http://localhost:3000/api/settings \
#   -H "Content-Type: application/json" \
#   -d '{
#     "action": "configure_tradingview",
#     "symbol": "BTCUSD",
#     "exchange": "COINBASE",
#     "timeframes": ["1h", "4h"]
#   }'
#
# View all settings:
# curl http://localhost:3000/api/settings
#
# ===========================================
